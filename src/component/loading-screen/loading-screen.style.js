import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  vLoading: {
    backgroundColor: '#FFF',
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },

  aiLoading: {
    marginTop: 20,
  },

  imgStartLoading: {
    width: 72,
    height: 70,
  },
});
