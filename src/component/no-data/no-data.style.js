import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  vNoData: {
    backgroundColor: '#FFF',
    alignItems: 'center',
    paddingVertical: 30,
  },

  vNoDataTransparent: {
    backgroundColor: 'transparent',
    alignItems: 'center',
    paddingVertical: 30,
  },

  imgNoData: {
    marginBottom: 20,
  },

  tNoData: {
    fontSize: 16,
    color: '#828282',
    textAlign: 'center',
  },
});
